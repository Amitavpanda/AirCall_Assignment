

export interface Chip {
    id: number;
    label: string;
    email: string;
    color: string;
}

export interface ListItem {
    id: number;
    title: string;
    email: string;
    color: string;
}
